# Binance market data (klines, ticker) is a PUBLIC API — no key required.
# This bot currently needs no API keys to run.
#
# If you later add private Binance features (account balance, order placement),
# add your keys here:
# BINANCE_API_KEY=your_key_here
# BINANCE_API_SECRET=your_secret_here
